# ── 密钥。这一行是整个文件里最重要的一行 ──
.env
*.key

# ── 依赖与缓存 ──
.venv/
.uv/
__pycache__/
*.pyc

# ── 运行时产物 ──
# checkpoint 库 55M 且含会话内容，本地跑一次就会重建
*.sqlite
ramp_checkpoints.sqlite
# 上一次 CLI 会话 id
.sid
# 成本采样中间结果，结论已写进 docs/cost-model.md
.route_costs.json
